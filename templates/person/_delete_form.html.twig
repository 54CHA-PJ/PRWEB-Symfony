<form method="post" action="{{ path('app_person_delete', {'person_id': person.getPersonId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ person.getPersonId) }}">
    <button class="btn">Delete</button>
</form>
